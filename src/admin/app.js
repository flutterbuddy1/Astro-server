const config = {
  translations: {
    en: {
      'app.components.LeftMenu.navbrand.title': 'Astrology Dashboard',
    }
  },

};

const bootstrap = (app) => {
  console.log(app);
};

export default {
  config,
  bootstrap,
};
